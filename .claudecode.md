# Personal Instructions
- Use memory mode (`#` command) to update project context.
- Prioritize concise, actionable advice over verbose explanations.
- Reference existing notes in `Ethereum/`, `Rust/`, and `AI/` before generating new content.
- Support current learning focus: NestJS (backend), Uniswap (V1-V4), and Solana (transactions/safe wallet).

## Antigravity Alignment
- **Tool Guard**: Antigravity MUST check `.claude/settings.json` and `.claude/managed-settings.json` before executing any `run_command` or tool. If a command or tool is marked as `allow: false` or is in a `deny` list, Antigravity must refuse to run it and inform the user.
- **Skill Priority**: Before performing any complex task, Antigravity MUST check `.claude/skills/` for relevant "Expert Instructions" and follow them as the highest priority.
- **Priority Hierarchy**: Follow the Claude Code priority: `Enterprise Settings` (.claude/managed-settings.json) > `Personal Settings` (.claude/settings.local.json) > `Project Settings` (.claude/settings.json).

## Local Development
- Any scripts created for note automation should be stored in `/tmp/` unless otherwise requested.
- Use `Think more` or `Ultrathink` for complex blockchain logic or auditing tasks.
