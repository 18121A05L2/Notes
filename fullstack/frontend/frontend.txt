--> updating package inside a package
    In this example, any instance of multer used by express-openapi-validator will be overridden to version 1.4.4-lts.1. This method is particularly useful for addressing vulnerabilities or ensuring consistency across your dependency tree
    {
        "overrides": {
            "express-openapi-validator": {
                "multer": "1.4.4-lts.1"
                }
        }
    }
-> Key headers associated with CORS are: ( we get all these from prefligh options call)
    - Acess-Control-Allow-Origin, Access-Control-Allow-Methods, Access-Control-Allow-Headers and Access-Control-Max-Age
-> npm ci stands for "clean install", and it's a command used to install dependencies exactly as specified in your package-lock.json

